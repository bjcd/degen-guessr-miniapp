{
    "name": "degen-guessr-subgraph",
    "version": "0.1.0",
    "description": "The Graph subgraph for DegenGuessr game",
    "scripts": {
        "codegen": "graph codegen",
        "build": "graph build",
        "deploy": "graph deploy --node https://api.thegraph.com/deploy/ degen-guessr",
        "create-local": "graph create --node http://localhost:8020/ degen-guessr",
        "remove-local": "graph remove --node http://localhost:8020/ degen-guessr",
        "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 degen-guessr"
    },
    "dependencies": {
        "@graphprotocol/graph-cli": "0.56.0",
        "@graphprotocol/graph-ts": "0.31.0"
    }
}
